//キーボードマクロのファイル
S_GoLineEnd(0);	// 行末に移動(折り返し単位)
S_GoLineTop_Sel(0);	// (選択)行頭に移動(折り返し単位)
S_Cut(0);	// 切り取り
S_Delete(0);	// 削除
S_Down(0);	// カーソル下移動
S_Char(13);	// 文字入力
S_Up(0);	// カーソル上移動
S_Paste(0);	// 貼り付け
