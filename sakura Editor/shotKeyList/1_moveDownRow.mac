//キーボードマクロのファイル
S_GoLineTop(0);	// 行頭に移動(折り返し単位)
S_GoLineEnd_Sel(0);	// (選択)行末に移動(折り返し単位)
S_Cut(0);	// 切り取り
S_Delete(0);	// 削除
S_Up(0);	// カーソル上移動
S_Char(13);	// 文字入力
S_Up(0);	// カーソル上移動
S_Paste(0);	// 貼り付け
